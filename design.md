# تصميم واجهة YouTube Downloader

## المبدأ والنطاق

التطبيق مخصص لتنزيل المحتوى الذي يملك المستخدم حق حفظه أو الذي يتيحه صاحبه للتنزيل. يعمل الهاتف كواجهة لإدخال الرابط واختيار صيغة الإخراج ومتابعة حالة الطلب. تتطلب عملية `yt-dlp` نفسها بيئة تشغيل لينكس قادرة على تنفيذ الأداة وFFmpeg؛ لذلك تُصمَّم طبقة اتصال مستقلة قابلة للربط بخدمة خاصة يملكها المستخدم، ولا تُضمَّن مفاتيح أو بيانات اعتماد في التطبيق.

## قائمة الشاشات

| الشاشة | المحتوى والوظيفة |
| --- | --- |
| تنزيل جديد | حقل رابط يوتيوب، لصق الرابط، فحص تنسيقه، اختيار النوع والجودة، وزر بدء التنزيل. |
| خيارات الصيغة | ورقة سفلية لاختيار فيديو MP4 أو صوت MP3/M4A أو الفيديو الأصلي، ثم دقة أو جودة الصوت. |
| التنزيلات | قائمة محلية بالطلبات مع حالات الانتظار والتنزيل والاكتمال والفشل، وشريط تقدم وإجراءات الإلغاء وإعادة المحاولة. |
| إعداد الخادم | عنوان خدمة التنزيل الشخصية فقط، مع اختبار اتصال واضح. لا تعرض الشاشة كلمة مرور أو ملفات تعريف ارتباط. |
| سياسة الاستخدام | شرح موجز لحق المستخدم في تنزيل المحتوى وإخلاء مسؤولية قبل أول طلب. |

## تخطيط وتجربة الاستخدام

يستهدف التصميم وضعية عمودية بنسبة 9:16 واستخدامًا بيد واحدة. توضع العملية الرئيسية في أعلى شاشة التنزيل الجديد: عنوان قصير، حقل رابط مرتفع يسهل لمسه، ثم خيارات على شكل شرائح اختيار بعرض الشاشة. يثبت زر **بدء التنزيل** في منطقة الإبهام السفلية فوق شريط التبويب. تستخدم بطاقات التنزيلات حوافًا دائرية معتدلة، وأشرطة تقدم دقيقة، ونصًا ثانويًا صغيرًا للحجم والحالة، مع عدم استخدام عناصر متحركة مشتتة.

شاشة التنزيلات تعتمد على قائمة افتراضية بسيطة عند عدم وجود عناصر، ثم قائمة مرتبة زمنيًا عند وجود الطلبات. يحتفظ التطبيق بقائمة المهام على الجهاز بحيث تبقى حالة الواجهة متاحة بعد إعادة فتح التطبيق، بينما تقرر خدمة التنزيل الفعلية الحالة النهائية عند تهيئتها.

## تدفقات الاستخدام الأساسية

| الهدف | خطوات المستخدم |
| --- | --- |
| إنشاء طلب جديد | يفتح المستخدم **تنزيل جديد** → يلصق رابطًا صالحًا → يختار صيغة وجودة → يوافق على الإقرار الحقوقي عند أول مرة → يضغط **بدء التنزيل** → ينتقل الطلب إلى قائمة التنزيلات. |
| متابعة طلب | يفتح المستخدم **التنزيلات** → يراجع التقدم والحالة → يلغي الطلب أو يعيد المحاولة عند الفشل. |
| ربط خدمة خاصة | يفتح المستخدم **الإعدادات** → يضيف عنوان الخدمة التي يملكها → يضغط اختبار الاتصال → يحفظ الإعداد عند نجاح الاختبار. |

## الألوان

| العنصر | اللون | الغرض |
| --- | --- | --- |
| اللون الأساسي | `#E84545` | تأكيد بدء التنزيل وعناصر التحديد. |
| اللون الداكن | `#151515` | العناوين والنص الرئيس في الوضع الفاتح. |
| الخلفية | `#FAFAF8` | مساحة هادئة تمنح البطاقات تباينًا ناعمًا. |
| السطح | `#FFFFFF` | البطاقات وحقول الإدخال. |
| لون النجاح | `#228B5B` | الطلبات المكتملة وحالة الاتصال. |
| لون التحذير | `#C47916` | تنبيه حقوق الاستخدام والحالات التي تحتاج إجراءً. |
| لون الخطأ | `#C73737` | أخطاء الرابط أو الخدمة. |

## البنية التقنية المقترحة

يحفظ التطبيق الإعدادات والمهام محليًا عبر `AsyncStorage`. يعرّف عميلًا بسيطًا لخدمة `yt-dlp` خاصة، بحيث يمكن أن يرسل طلب إنشاء مهمة ويتابع حالتها. لا يمكن لعميل Expo وحده تشغيل `yt-dlp` أو FFmpeg كأوامر نظام داخل حزمة أندرويد؛ ولهذا ستبقى الواجهة قابلة للاستخدام والاختبار دون خدمة، وستعرض حالة تهيئة واضحة بدل محاكاة تنزيل مزيف. عند ربط الخادم، تنفذ الخدمة المؤهلة عمليات التنزيل الفعلية وتعيد عنوانًا مؤقتًا أو ملفًا يمكن حفظه على الجهاز.
