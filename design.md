# خطة تصميم تطبيق «سراج الولاية»

## الاتجاه العام

«سراج الولاية» قارئ عربي محلي لتراث النبي وأهل البيت عليهم السلام، يربط المكتبة النصية بتقويم 1448هـ. التصميم مخصص للشاشة العمودية بنسبة **9:16**، واتجاهه الأساسي من اليمين إلى اليسار، ويعتمد مبادئ Apple Human Interface Guidelines: مساحات مريحة، أهداف لمس لا تقل عن 44 نقطة، أزرار نصية واضحة، وتدرج بصري هادئ يقدّم النص على الزخرفة.

الهوية البصرية ليست زخرفية ثقيلة؛ تعتمد خلفية عاجية دافئة، أخضر زمردي عميق للثقة والهدوء، ولمسة ذهبية ترابية للمناسبات والعناصر المهمة. لا تستخدم أيقونات أو صورًا لأشخاص مقدسين.

| الرمز | اللون | الاستخدام |
| --- | --- | --- |
| `ink` | `#142A2A` | عناوين النص وصورة التطبيق الداكنة. |
| `jade` | `#0C6B61` | الإجراء الأساسي، التبويب النشط، الوسوم الموثقة. |
| `gold` | `#B88737` | المناسبات، المؤشرات، والتفاصيل الرفيعة. |
| `paper` | `#F7F3EA` | الخلفية الفاتحة ومساحة القراءة. |
| `mist` | `#E9E2D2` | البطاقات والفواصل الرقيقة. |
| `night` | `#081B1D` | الوضع الداكن وخلفية وضع القراءة. |

## قائمة الشاشات

| الشاشة | المحتوى الأساسي | الوظيفة |
| --- | --- | --- |
| الرئيسية | بطاقة اليوم الهجري، أقرب مناسبة، نص مختار، اختصارات الأقسام. | نقطة دخول قصيرة للقراءة والتقويم. |
| المكتبة | أقسام الأدعية والزيارات والخطب والأقوال والوصايا والمناقب. | تصفح الحزم النصية المحملة محليًا. |
| قارئ النص | عنوان، نص، مصدر، موضع، ووسوم حالة النسبة. | قراءة هادئة وحفظ في المفضلة. |
| التقويم | أشهر 1448هـ، أيام الشهر، الأحداث وملاحظات الأهلة. | استعراض مناسبات وأعمال كل يوم. |
| تفاصيل اليوم | التاريخ الهجري/الميلادي، أحداث اليوم، رابط الأعمال ذات الصلة. | تحويل الحدث إلى خطة قراءة أو زيارة. |
| البحث | حقل بحث ومرشحات وقائمة نتائج. | بحث محلي في النصوص والعناوين. |
| المفضلة | مواد محفوظة محليًا. | العودة إلى النصوص المنتقاة. |
| الإعدادات | حجم النص، وضع القراءة، توضيح المصدر، حول البيانات. | تخصيص التجربة وإظهار الشفافية. |

## تدفقات الاستخدام الرئيسة

**القراءة اليومية:** يفتح المستخدم الرئيسية ← يرى تاريخ 1448هـ وأحداث اليوم ← يضغط «افتح المناسبة» ← يقرأ الأعمال أو النص المرتبط ← يضيفه إلى المفضلة.

**الوصول إلى نص موثق:** يفتح المكتبة ← يختار القسم ← يفتح سجلًا ← يرى النص واسم المصدر والموضع وحالة النسبة ← يحفظه أو ينسخ المرجع.

**تصفح التقويم:** يفتح التقويم ← يختار شهرًا ← يضغط يومًا يحمل أحداثًا ← ينتقل إلى تفاصيل اليوم ← يفتح مادة مرتبطة من المكتبة عند توفرها.

**تنظيم شخصي:** يفتح نصًا ← يضغط رمز الحفظ ← يظهر النص في تبويب المفضلة ← يمكن حذفه من المفضلة لاحقًا من دون تغيير البيانات الأصلية.

## نمط التفاعل

تظهر أهم الإجراءات في أسفل الشاشة أو ضمن شريط تنقل سفلي بخمسة عناصر: الرئيسية، المكتبة، التقويم، البحث، المفضلة. تستخدم البطاقات لمسة شفافة خفيفة وحدودًا ذهبية دقيقة. يكون الضغط على عناصر القوائم بتغيّر شفافية بسيط، والإجراء الأساسي بتصغير لا يتجاوز `0.97`. يظهر وضع القراءة في ورق عاجي أو ليل هادئ مع تحكم مباشر بحجم الخط.

## نموذج البيانات المحلي

| الكيان | الحقول الجوهرية | المصدر |
| --- | --- | --- |
| `HeritageEntry` | `id`، `title`، `text`، `speaker`، `topic`، `sourceName`، `sourceLocator`، `attributionStatus`، `tags`. | ملفات JSON المجمعة. |
| `CalendarMonth` | الاسم، الترتيب، عدد الأيام، توقع الهلال، الأيام. | `hijri_calendar_1448_complete.json`. |
| `CalendarDay` | اليوم الهجري، التاريخ الميلادي، `isoDate`، الأحداث. | ملف التقويم المرفق. |
| `AppPreferences` | حجم الخط، الوضع، المفضلة، آخر موضع قراءة. | تخزين محلي للجهاز. |

## النسخة الأساسية المنفذة

تبدأ النسخة الأولى بمكتبة محلية، وقارئ مادة، وبطاقة يوم هجري وتقويم شهري، وبحث محلي، ومفضلة محلية، وشاشة شفافية للمصدر. تبقى الميزات الموسعة المدرجة في `feature-roadmap.md` خارطة طريق لاحقة، وليست وعودًا بإدخالها جميعًا في الإصدار الأول.
